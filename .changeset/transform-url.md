---
'@rtorcato/supabase-common': minor
---

Add `transformUrl` — a pure builder for Supabase's image render endpoint (width/height/resize/quality/format), alongside the existing Storage URL builders.
