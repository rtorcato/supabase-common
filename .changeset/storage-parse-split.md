---
'@rtorcato/supabase-common': minor
---

Add `parsePublicUrl` (inverse of `publicUrl` → `{ bucket, path }`) and `splitPath` (`{ dir, name, ext }`), completing the pure Storage-path helpers from #38.
