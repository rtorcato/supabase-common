---
'@rtorcato/supabase-common': minor
---

Add unverified JWT claim helpers — `decodeClaims`, `getUserId`, `getRole`, `isExpired` (base64url decode of the access-token payload, no client). Closes #32.
