---
'@rtorcato/supabase-common': minor
---

Add Realtime convention builders — `channelName(schema, table, id?)` and `postgresChangesFilter({ event?, schema?, table, filter? })` (pure, no client). Closes #33.
