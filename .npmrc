# Emit npm provenance attestations when publishing from CI. The release job in
# .github/workflows/ci.yml grants `id-token: write`, which npm uses to sign the
# published package with a link back to the exact commit + workflow run.
# Publish-only: ignored during install.
provenance=true
